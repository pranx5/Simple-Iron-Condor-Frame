[project]
name = "iron-condor-desktop"
version = "1.0.0"
description = "0DTE Iron Condor desktop calculator for SPX/QQQ"
requires-python = ">=3.10"
dependencies = [
  "PySide6>=6.8",
  "matplotlib>=3.8",
]

[tool.setuptools]
packages = ["iron_condor"]
